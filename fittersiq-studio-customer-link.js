// Disabled safety stub. Build 72 was rolled back because its observer could loop.
// This file intentionally does nothing so any stale Studio page that still requests it
// cannot freeze the app while the browser refreshes to the current Stage 1 build.
(function(){ return; })();
