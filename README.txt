AssembleOne Version 12.7

Homepage and workflow update:
House restored as Home icon.
Drawing uses a measuring ruler icon.
My Templates removed from the homepage.
Site Jobs is now the fourth homepage action.
Template and Hardware Check moved to the Finish Job page before sending to phone.

AssembleOne Version 12.4

Homepage images are now embedded directly inside Studio.html, so the illustrated cards work when the file is opened locally or uploaded without a separate assets folder.

ASSEMBLEONE VERSION 11.0.1 SYNCHRONISED CLOUD BUILD

Upload these three files together to the ROOT of the GitHub repository and replace the existing files:

1. Studio.html
2. Mobile.html
3. index.html

The existing project.json can remain where it is. It is not required to open Studio or Mobile.

This corrected package keeps Studio and Mobile on the same Firebase cloud channel:
mads_assembleone_1089

The root GitHub Pages address opens Mobile.html?v=11.0.1 so the phone does not reuse the older cached page.

Studio address:
Studio.html?v=11.0.1

Phone address:
Mobile.html?v=11.0.1

After GitHub Pages has deployed, close every old AssembleOne tab on the iPhone. Open the main GitHub Pages address in a new Safari tab. The header should show Phone v11.0.1 · Cloud Sync.


Version 12.3
New illustrated Studio homepage with colourful carpentry artwork, customer couple icon, wardrobe project cards, improved spacing and updated recent jobs presentation. Existing project, QR, cutting list and phone synchronisation functions remain unchanged.


Version 12.5 sidebar update:
- Folder icon is now the Home button.
- The old house icon has been replaced with a drawing/ruler icon for the Drawing section.
- Existing screen functions and synchronisation are unchanged.

Version 12.7: New illustrated Site Jobs card, compact language menu, corrected active project status, and corrected Studio version display.
